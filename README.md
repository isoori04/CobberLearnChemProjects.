Using the matplotlib database, Chat GPT was utilized to create a graph of boiling points of alkanes with increasing number of carbon atoms
In addition to this public file, I will maintain a private Ethics portfolio where I reflect on issues and decide what kind of scientist I want to become

In ch5 twist, with help of AI, a script was created to display cursory chemical information regarding a compound. My twist was that it opens a 2d visualization of your chosen molecule when you run the script.

IN ch6, two descriptors were added, HBA and Crippen. For the twist, I asked the agent to clean up the output and put the data output into a neat table.

In chapter 7, we learned how to calculate imputations based on the incomplete age data for passengers aboard the Titanic. My twist was to organize the data further.

In chapter 8, residual plots were created. They were then improved by making the worst prediction obvious, improved plots, and worst row in the table made obvious.

CHapter 9: Built and trained a DecisionTreeClassifier in scikit-learn to predict water solubility (0/1) from molecular features (molecular weight and hydrogen-bond donor/acceptor counts). Visualized and saved the learned decision rules with plot_tree, then experimented with model customization (feature selection, max_depth, and intentionally changing a label) to see how the tree adapts.(generated with Chat GPT assistance)

Ch10: This program lets you choose a periodic table group (1–8), then automatically builds a CSV file with element name, symbol, atomic number, atomic radius, and first ionization energy for that group. It loads the data with pandas, checks/cleans it, saves a fixed version, and then runs interactive KMeans clustering where you can try different k values and see how elements are grouped. For each k, it creates a scatter plot (atomic radius vs. ionization energy), colors points by cluster, labels element symbols, and marks cluster centers as large black X symbols.
Ch13: This project explores how well molecular weight alone can be used to predict boiling point by comparing a simple linear regression model with customizable neural network architectures built in Python using pandas and scikit-learn. It includes model training, error analysis, holdout testing on an unseen compound, and visual comparisons of predictions to show both the strengths and limitations of machine learning when applied to a small chemistry dataset.
Weasel:e program uses a fitness function (character-by-character matching), mutation (5% chance per character), and selection (keeping the best offspring each generation) to gradually improve the phrase over time. I also added improvements including a fitness-over-time plot, automatic saving of the plot, a generation log text file, and an option to enter a custom target phrase. This project helped me understand how cumulative selection makes evolutionary algorithms much more effective than pure random guessing.( GPT)
Week 9:This code creates a small black-and-white image as a NumPy array and defines filters that detect horizontal, vertical, and diagonal patterns. It applies those filters using convolution, prints the resulting feature maps, and also includes a manual convolution function to show how the process works step by step. It then visualizes the original image and the detected features with matplotlib.
Week9.2:This code imports the required libraries, loads the pre-trained VGG16 model with ImageNet weights, and prepares an image in the format the CNN expects. It then predicts the object in the image, prints the top five class labels with confidence scores, and applies a 70% confidence threshold to decide whether to accept the top prediction.
Ch15: This code trains a Q-learning agent to solve the FrozenLake-v1 environment by updating a Q-table based on rewards and future expected values from the Bellman equation. After training, it prints the learned policy, tests the agent’s success rate over 100 episodes, plots learning progress, and saves the trained Q-table for later use.
Ch18:This code creates and runs the CartPole-v1 reinforcement learning environment, where the agent repeatedly observes the state, chooses an action, and records the reward and number of steps until the episode ends. It compares a random policy to a simple rule-based policy, prints performance results, and generates graphs to show how each approach performs over multiple episodes.
Ch16:This project uses the California housing dataset to compare four regression models—Linear Regression, Decision Tree, Random Forest, and Support Vector Regressor—to predict median home values and determine which approach performs best. It also includes data visualizations such as feature histograms, a correlation heatmap, predicted-vs.-actual plots, and feature-importance analysis to help explain the patterns driving housing prices. To make the program more practical and interactive, the script supports hyperparameter tuning, cross-validation, sample predictions for made-up houses, and a custom input loop so users can test their own housing scenarios.  GUI:This project builds a Tkinter-based graphical interface for the California housing regression model so users can predict median home prices without typing commands into the terminal. The GUI retrains a Random Forest model at startup, accepts all key housing features as inputs, and displays predictions in a clean, user-friendly format with validation, default values, tooltips, and reset options. It demonstrates how machine learning models can be made more accessible by combining predictive power with an interactive dashboard.
CH17: This project uses K-means clustering on the classic Iris dataset to group flowers based only on their measurements, without giving the algorithm the species labels ahead of time. The program visualizes the clusters with 2D and 3D plots, compares them to the real species, and reports how well the unsupervised model discovered the natural patterns in the data. It also includes enhancements such as optional feature scaling and testing different numbers of clusters to make the analysis more informative and flexible.
CH20:This program compares an experimental protein structure to its AlphaFold prediction by aligning the two PDB files, calculating RMSD, and measuring per-residue differences. It also plots structural deviations and AlphaFold pLDDT confidence scores, then creates aligned PDB files plus both PyMOL and browser-based visualizations so you can inspect where the structures match or differ.
